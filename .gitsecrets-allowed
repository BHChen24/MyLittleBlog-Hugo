# Allowed (whitelisted) patterns for git-secrets.
#
# Empty by design: no Algolia credential appears in the repository any more.
# Both the app ID and the search-only key come from HUGO_PARAMS_* environment
# variables at build time, so the prohibited patterns need no exceptions.
#
# Add one literal string per line here if a future pattern ever needs one.
